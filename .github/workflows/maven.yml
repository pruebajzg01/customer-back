# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: build and test feature branch

on:
  push:
    branches:
      - feature/*

jobs:
  build-feature:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: pull and checkout  from git repository4
        
        run: |
          echo "se está haciendo un pull y checkout desde el repostiorio de git--jeje"
        
      - name: usando action para java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin' # See 'Supported distributions' for available options
          java-version: '17'

      - name: maven build clean and install
        run: |
          mvn clean install
          echo "se hizo clean and install con maven"
  